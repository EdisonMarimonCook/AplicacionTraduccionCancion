<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<<<<<<< HEAD
=======
    <!-- Permisos de Red -->
>>>>>>> feature/lyrics-translation
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
<<<<<<< HEAD
        android:theme="@style/Theme.Diccionario_HipHop"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
=======
        android:theme="@style/Theme.DiccionarioHipHop"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- 0. SPLASH SCREEN (Nueva Portada - LAUNCHER) -->
        <activity
            android:name=".SplashActivity"
>>>>>>> feature/lyrics-translation
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<<<<<<< HEAD
        <!-- Song Selection Activity -->
        <activity
            android:name=".SongSelectionActivity"
            android:exported="false"
            android:label="@string/select_song"
            android:parentActivityName=".MainActivity" />

        <!-- Song Learning Activity -->
        <activity
            android:name=".SongLearningActivity"
            android:exported="false"
            android:label="@string/song_learning"
            android:parentActivityName=".SongSelectionActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:parentActivityName=".MainActivity" />
=======
        <!-- 1. Login (Ya no es Launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 2. Registro -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- 3. Selección de Canciones (Home) -->
        <activity
            android:name=".SongSelectionActivity"
            android:exported="false" />

        <!-- 4. Aprendizaje -->
        <activity
            android:name=".SongLearningActivity"
            android:exported="false" />

        <!-- 5. Diccionario -->
        <activity
            android:name=".DictionaryActivity"
            android:exported="false" />

        <!-- 6. Perfil -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!-- 7. Flashcards -->
        <activity
            android:name=".FlashcardsActivity"
            android:exported="false" />
>>>>>>> feature/lyrics-translation

    </application>

</manifest>